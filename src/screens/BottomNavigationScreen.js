import {View} from 'react-native';
import {Text} from 'react-native-paper';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function BottomNavigationScreen() {
  return (
    <SafeAreaView>
      <View>
        <Text>Bottom Navigation Screen</Text>
      </View>
    </SafeAreaView>
  );
}
